## 注意事项

### 1. **概念处理**

在项目开发过程中，为了保持术语使用的一致性，避免团队成员之间产生不必要的误解，需统一以下几个重要概念的称呼：

### 1.1 **类与结构体**

- **类（Class）与结构体（Struct）视为同一概念**：在项目中，无论你使用的是类（Class）还是结构体（Struct），都视为同一种数据结构。
    - 在大多数面向对象语言中（如 Java、Kotlin、Dart），我们使用 **类** 来定义对象模型。
    - 对于某些语言（如 Go、C++、Swift 等），**结构体** 可能是常用的数据结构，但在此项目中，我们将它们视为具备相同功能的概念。

### 1.2 **函数与方法**

- **函数（Function）与方法（Method）**：方法是一个函数，但是不是所有函数是方法，所以函数是一个贴切的称呼。
    - **函数（Function）** 通常指的是独立的代码块，可以在全局或某个模块中定义，并独立执行。
    - **方法（Method）** 通常是类的成员函数，绑定在类或对象上，用于操作类的属性或执行与对象相关的操作。
    - **统一称呼**：在文档中，我们不区分 Function 和 Method，统一称为 **函数** 或 **成员函数**。这意味着无论是类的成员方法，还是全局函数，都可以使用函数这个术语。

### 1.3 **使用"成员函数"或"函数"来称呼方法**

- **成员函数**：当我们需要强调某个函数是类的成员时，使用 **成员函数** 这个术语。
    - 例如，在服务类中定义的业务逻辑操作，可以称为 **成员函数**。
- **函数**：对于一般的函数（无论是全局的还是类中的），我们可以直接称为 **函数**，以简化术语的使用。
    - 例如，工具类中的某些通用功能函数，可以称为 **工具函数**。

---

## 1. **代码风格与格式化**

### 1.1 缩进

- **统一缩进**：所有项目中应使用一致的缩进方式，确保代码风格统一，便于阅读和维护。
    - **JavaScript 和 Java** 推荐使用 **4 格空格缩进**。
    - **Flutter/Dart** 推荐使用 **2 格空格缩进**。

### 1.2 大括号

- **大括号内联**：大括号可以内联（即与控制语句或函数声明位于同一行），但这不是硬性规定。团队应根据实际情况统一风格，允许适当的灵活性。
    - 示例（内联大括号）：
        
        ```java
        if (condition) {
            // code block
        }
        ```
        

### 1.3 使用编辑工具的格式化功能

- **自动格式化工具**：建议开发者通过 IDE 或编辑器提供的**自动格式化工具**（如 IntelliJ、VSCode、Android Studio 等）来保持代码格式的统一。
    - **注意**：在团队中应配置统一的格式化规则，确保所有成员的代码风格一致。

### 1.4 空白行与换行

- **空白行**：在逻辑上相对独立的代码块之间适当加入空白行，以增强代码的可读性。
- **适当换行**：避免过长的代码行，通常建议一行代码不超过 80-100 个字符，根据语言和团队规范适当换行。

---

## 2. **命名规范**

这些规范，除非指定的语言有特别要求，则应遵守。

**核心原则原则：命名必须让人一眼就能看懂其用途和含义。**

- 如果需要花时间去猜这个变量/函数是干什么的，命名就是失败的
- 命名比注释更重要，好的命名不需要注释就能理解
- 宁愿写长名字也不要写不清楚的短名字
- 避免使用只有团队内部才能懂的缩写或黑话

### 2.1 变量与常量

- **使用有意义的变量名**：变量名应能清楚表达其含义，避免使用无意义的缩写或单字母命名。
    - 变量名应该回答"这是什么？"
    - **错误示例**：

        ```java
        int a = 42;
        int d = calculate(x, y);
        ```

    - **正确示例**：

        ```java
        int userAge = 42;
        int discountAmount = calculate(originalPrice, discountRate);
        ```
        
- **变量与常量使用场景**：
    - **JavaScript**: 永远不要使用 `var`，局部变量使用 `let`，常量使用 `const`。
    - **Java**: 使用 `final` 声明不可变的常量。

### 2.2 类与函数命名

- **类名使用大驼峰命名法**：类名应使用大驼峰命名法（PascalCase），并清楚表达其功能或用途。
    - 类名应该能让人一眼看出它是什么，而不是它的行为。
    - 错误示例：`Manager`, `Helper`, `Utils`（太泛泛）
    - 正确示例：`UserRepository`, `OrderService`, `PaymentProcessor`
    - 示例：
        
        ```java
        public class UserController {
            // class body
        }
        ```
        
- **函数名使用小驼峰命名法**：函数名应使用小驼峰命名法（camelCase），动词开头，清晰描述该函数的功能。
    - 函数名应该表达它**做什么**，而不是**怎么做**。
    - 错误示例：`processData`, `handleRequest`,虽然常用但信息量不够
    - 正确示例：`validateUserInput`, `calculateOrderTotal`, `sendNotificationEmail`
    - 常用动词前缀：
        - `get`/`fetch`: 获取数据
        - `set`/`update`: 设置或更新数据
        - `create`/`add`: 创建或添加数据
        - `delete`/`remove`: 删除或移除数据
        - `validate`/`check`: 验证或检查
        - `calculate`/`compute`: 计算值
        - `send`/`notify`: 发送或通知
        

### 2.3 常量命名

- **常量使用全大写加下划线**：常量名应全部大写，单词之间使用下划线分隔。
    - 示例：

        ```java
        public static final int MAX_USER_COUNT = 100;
        ```

### 2.4 文件命名

- **文件名使用小驼峰**：文件名使用小驼峰命名法（camelCase），与类名保持一致（去掉类名的大驼峰）。
    - 示例：
        - 类名 `UserController` → 文件名 `userController.ts`
        - 类名 `UserService` → 文件名 `userService.go`

- **测试文件命名**：测试文件应与被测试文件同名，后缀加 `_test` 或 `.test`。
    - 示例：
        - `userService.go` → `userService_test.go`
        - `userService.ts` → `userService.test.ts`

### 2.5 包与目录命名

- **包名使用小写单词**：包名使用小写，通常一个单词，避免下划线和驼峰。
    - 好的示例：`user`, `auth`, `config`
    - 不好的示例：`user_service`, `User`, `user-service`

- **目录名使用小写或短横线**：目录名使用小写或短横线分隔多个单词。
    - 示例：`components/`, `user-service/`, `api-client/`

### 2.6 接口命名

- **接口名使用大驼峰，前缀或后缀表示接口**：
    - Java 风格：`UserService`（接口），`UserServiceImpl`（实现）
    - Go 风格：`UserRepository`（接口），结构体实现不需要特殊命名
    - TypeScript 风格：`IUserService`（接口），`UserService`（实现）

### 2.7 私有成员命名

- **私有成员使用前缀或特定语法**：根据语言特性使用合适的方式。
    - **Java**: 使用 `private` 修饰符，命名风格与其他成员一致
    - **JavaScript/TypeScript**: 使用 `_` 前缀（如 `_privateMethod`）
    - **Go**: 使用小写字母开头自动为私有
    - **Dart**: 使用 `_` 前缀

### 2.8 布尔变量命名

- **布尔变量使用问号形式或谓语形式**：清晰表达是/否状态。
    - 好的示例：`isActive`, `hasPermission`, `canEdit`, `shouldUpdate`
    - 不好的示例：`flag`, `status`, `isFlag`

### 2.9 临时变量命名

- **循环变量使用简短但有意义的名称**：
    - 索引：`i`, `j`, `k`（嵌套循环）
    - 元素：`item`, `user`, `product`（避免使用 `obj`, `data`）

- **异常捕获变量使用有意义的名称**：
    - 好的示例：`err`, `error`, `exception`
    - 不好的示例：`e`, `ex`, `x`

### 2.10 避免的命名

- **避免无意义的单字母变量**（循环索引除外）
- **避免缩写**：除非是广泛认知的缩写（如 `id`, `url`, `api`, `http`, `sql`）
    - 错误示例：`usrNm`, `pwd`, `cnt`
    - 正确示例：`userName`, `password`, `count`
- **避免使用拼音**：变量名应使用英文
- **避免使用保留字**：不要使用语言的保留关键字作为变量名
- **避免泛泛的名称**：
    - 错误示例：`data`, `info`, `result`, `item`, `obj`
    - 正确示例：`userData`, `userInfo`, `searchResult`, `orderItem`, `user`
- **避免在名称中包含类型**：现代 IDE 有类型提示
    - 错误示例：`userString`, `countInt`, `isActiveBool`
    - 正确示例：`userName`, `count`, `isActive`


---

## 3. **逻辑处理与控制流**

### 3.1 减少嵌套层级

- **避免深层嵌套**：尽量减少代码的嵌套层级，嵌套过深会使代码难以阅读和维护。可以通过提前返回（early return）等方式简化逻辑。
    - **错误示例**：
        
        ```java
        if (condition1) {
            if (condition2) {
                if (condition3) {
                    // deeply nested code logic
                }
            }
        }
        
        ```
        
    - **正确示例**：
        
        ```java
        if (!condition1) {
            return;
        }
        if (!condition2) {
            return;
        }
        if (!condition3) {
            return;
        }
        // simplified code logic
        
        ```
        

### 3.2 避免过度递归

- **避免递归的滥用**：递归会带来性能问题，尤其是在层级较深时，容易导致栈溢出。优先使用循环替代递归，除非递归是最清晰的解决方案。

### 3.3 条件判断

- **减少复杂的条件判断**：如果条件非常复杂，考虑将条件逻辑提取到独立的函数中，提升代码的可读性。
    - **错误示例**：
        
        ```java
        if ((userAge > 18 && userAge < 60) || (userType == "admin" && isActive)) {
            // complex condition logic
        }
        ```
        
    - **正确示例**：
        
        ```java
        if (isEligibleForAccess(userAge, userType, isActive)) {
            // simplified logic
        }
        
        private boolean isEligibleForAccess(int age, String type, boolean isActive) {
            return (age > 18 && age < 60) || (type.equals("admin") && isActive);
        }
        ```
        

---

## 4. **函数设计**

### 4.1 函数长度

- **保持函数短小**：一个函数的长度应尽可能简短，通常不应超过 30-40 行代码。若函数过长，考虑将其拆分为多个子函数。

### 4.2 单一职责原则

- **单一职责**：每个函数应只完成一项任务，避免一个函数承担过多职责。

### 4.3 参数数量

- **减少函数参数**：函数参数的数量应尽量控制在 3 个以内，超过 3 个参数时，考虑将这些参数封装到对象中。

---

## 5. **错误处理**

### 5.1 异常处理

- **明确异常处理**：在设计函数时，明确可能抛出的异常，并确保调用者能清楚如何处理这些异常。
    - **Java** 中，使用 `try-catch` 块捕获异常，避免程序崩溃。
    - **JavaScript** 中，尽量使用 `async/await` 和 `try-catch` 捕获异步函数中的错误。

### 5.2 日志记录

- **错误日志记录**：捕获到异常时，**应记录详细的错误日志**，以便调试和维护。

---

## 6. **安全性**

### 6.1 敏感信息处理

- **安全存储敏感信息**：敏感信息（如密码、API 密钥等）不应硬编码在代码中，而应使用安全的存储方式（如环境变量或加密存储）。

### 6.2 输入验证

- **验证输入数据**：无论是用户输入还是外部 API 的返回数据，都应进行验证，防止恶意输入或数据错误导致的安全漏洞。

---

## 7. **代码结构与模块化**

### 7.1 模块化设计

- **功能模块化**：将功能划分为独立的模块或类，避免将所有代码堆积在一个文件中。每个模块应负责单一的功能或逻辑部分。
    - **Flutter** 中，各个页面放在 `pages` 文件夹，网络相关代码放在 `network` 文件夹，复用组件放在 `widgets` 文件夹。
    - **JavaScript** 中，按功能模块将文件分开，避免臃肿的单一文件结构。

### 7.2 依赖注入

- **依赖注入**：避免在代码中直接创建依赖对象，使用依赖注入（Dependency Injection）来管理对象的创建和生命周期。这样可以提高代码的灵活性和可测试性。
    - **Java** 中，使用框架如 Spring 来实现依赖注入。
    - **Flutter** 中，使用 `Provider` 或 `GetX` 来管理依赖注入。

---

## 8. **版本控制**

### 8.1 使用版本管理工具

- **版本管理工具**：对项目的依赖和工具版本进行管理，保证项目在不同环境中的一致性。
    - **Flutter** 项目中使用 FVM（Flutter Version Management）管理 Flutter SDK 版本。
    - **JavaScript** 项目中使用 `nvm` 或者 `volta` 来管理 Node.js 版本，避免版本冲突。

### 8.2 `.gitignore` 配置

- **配置 `.gitignore`**：确保项目的 `.gitignore` 文件配置正确，避免将不必要的文件（如编译生成文件、依赖缓存等）提交到版本控制系统中。

---

## 9. **测试与调试**

### 9.1 测试编写

- **编写测试用例**：对关键功能编写单元测试（Unit Test）和集成测试（Integration Test），确保代码的健壮性和可维护性。
    - **Java** 中，使用 JUnit 或 TestNG 编写测试。
    - **Flutter** 中，使用 `flutter_test` 包编写测试。

### 9.2 调试日志

- **适当使用调试日志**：在开发过程中，适当使用日志记录调试信息，避免将调试信息直接输出到控制台。

---

通过遵循此通用前置编码文档，团队可以在项目开发中保持一致的代码风格和高质量的编码标准。使用自动格式化工具来保持代码格式的一致性，并根据团队的实际需求灵活调整部分代码风格，可以有效提高项目的可读性和可维护性。
