# 设计规范

编辑: lmt
标签: 指引和帮助, 设计

<aside>
💡 JImi Hendrix 曾经说过: Oh shit, shit shit shit.

</aside>

设计规范是一套关于视觉设计、交互行为、代码结构等方面的规则和标准。它可以涵盖从颜色、字体、按钮样式到交互模式、响应式布局甚至可访问性原则等多个方面。设计规范的核心目的是保证在多人协作开发中，所有 UI 元素和交互行为保持一致，从而为用户提供连贯的体验。

这套设计规范适用于前端全端开发。

## 设计规范在前端开发中的重要性

[论设计规范在前端开发中的重要性](%E8%AE%BA%E8%AE%BE%E8%AE%A1%E8%A7%84%E8%8C%83%E5%9C%A8%E5%89%8D%E7%AB%AF%E5%BC%80%E5%8F%91%E4%B8%AD%E7%9A%84%E9%87%8D%E8%A6%81%E6%80%A7%2012bd210df36f80a9ab7cfbef09b79a2d.md)

# 正文

## 基本原则

### 尺寸单位

使用 dp 作为基本单位。

- **dp (Density-independent Pixels)** 是与设备屏幕的物理像素密度无关的单位，能够确保在不同分辨率和屏幕尺寸的设备上，UI 元素的尺寸保持一致。
- 在 Android 开发中，`dp` 是推荐的单位。它根据设备的 `dpi`（dots per inch）进行缩放，确保元素在不同设备上有相似的物理尺寸。
- 在 iOS 中，对应的概念是 `pt`（points），与 `dp` 的作用类似。在跨平台设计中，`dp` 和 `pt` 通常被认为是等价的。
- 在 Android 中，1 dp 在 160 dpi 的显示器上等于 1 像素，但在更高分辨率的屏幕上会自动缩放。
- 下文提到的  `px` 均为 `dp` 。

### 布局方式

- 使用栅格系统作为布局方式。
    
    [栅格系统](%E6%A0%85%E6%A0%BC%E7%B3%BB%E7%BB%9F%2012bd210df36f8059aa23d69b10d247eb.md)
    
    - 使用 8px 或 4px 的栅格系统。这意味着所有的间距、尺寸和元素的大小都应该是 8px 或 4px 的倍数。例如：按钮高度 48px、间距 16px 等。
- **列布局**：常见的列布局包括 12 列、16 列或 24 列布局，适用于响应式设计。
    - **宽度设置**：列的宽度应根据屏幕宽度自适应调整。
    - **间距 (Gutter)**：列与列之间的横向间距，通常设置为栅格系统的倍数，如 `16px` 或 `24px`。

### **图像和图标单位**

图像和图标的尺寸和分辨率也应该有明确的规范，确保它们在不同设备上清晰显示。

- **图片尺寸**：图片的尺寸应根据实际设计比例进行设置，确保在高分辨率设备上显示清晰。
    - **2x 或 3x 尺寸**：为适应高分辨率屏幕（如 Retina 屏幕），图片和图标通常应导出 2x 或 3x 的尺寸。例如，在设计图中显示为 100px 的图片，实际导出时应为 200px 或 300px。
- **SVG 图标**：推荐使用可缩放矢量图形 (SVG)，确保图标在不同分辨率设备上都能保持清晰度。

### **边距与内边距单位**

- **全局边距标准**：所有元素的外边距（`margin`）和内边距（`padding`）应按照栅格系统的倍数进行设置，通常是 `8px` 或 `16px` 的倍数。
- **可响应的间距**：边距和内边距应根据屏幕尺寸进行适当调整。例如，在较小设备上使用较小的边距和内边距，而在大屏设备上则适当增加。

**边框和分割线**

- **边框和分割线**：通常使用 `1px` 作为基础单位，以确保线条在不同设备上清晰可见。
    - **高分辨率设备上的线条**：为了在高 DPI 设备上显示精细的线条，推荐使用半透明的 `1px` 线条，或者在设计时考虑 `0.5px` 的线宽。

## **响应式设计原则**

响应式设计的核心是确保界面在不同屏幕尺寸下都能正确显示。通过定义断点和基于单位的调整，可以确保 UI 在各种设备上保持一致性。

- **断点规范**：
    
    > 断点是指当屏幕宽度达到某个特定值时，页面布局、字体大小、间距等样式发生变化的点。
    断点决定了布局如何根据设备的屏幕宽度进行调整。
    > 
    - **小屏设备 (Mobile)**：0px - 576px
    - **中屏设备 (Tablet)**：576px - 768px
    - **大屏设备 (Desktop)**：768px - 1200px
    - **超大屏设备 (Large Desktop)**：1200px 以上

![image.png](image.png)

你可以根据项目实际情况调整断点的具体值。

## 间距规范

### 容器间距

- **页面外边距（Page Margins）**：
    - 移动端：12**px**（左右）和 12**px**（上下）
    - 平板与桌面端：12**px**（左右）和 16**px**（上下）
    

![image.png](image%201.png)

- **内容容器的内部边距（Padding within Containers）**：
    - 通常为 12**px** 或 16**px**，根据具体的内容密度和视觉层次决定。
    - 例如，卡片组件、表单容器、或弹出框的内部边距一般为 12**px**。
    

![image.png](image%202.png)

## 图标规范

- 小型图标的基本尺寸长宽均为 `24px` 。
    - 例如状态栏图标
- 中型图标的基本尺寸长宽均为 `48px` 。
    - 例如启动图标

## 文字规范

## 字体

- **主字体**：思源黑体（Noto Sans CJK SC / Source Han Sans SC）
- **备用字体**：`sans-serif`（用于设备不支持思源黑体的情况）

### **字体大小规范**

字体大小应该遵循层次性和一致性，确保用户在阅读时能够清晰辨别不同级别的文本。

### 标题（Headings）

| 级别 | 字体大小（px） | 行高（px） | 字重（Weight） | 用途描述 |
| --- | --- | --- | --- | --- |
| H1 | 32px | 40px | 700 | 页面主标题 |
| H2 | 30px | 38px | 600 | 重要副标题 |
| H3 | 26px | 34px | 600 | 次级标题 |
| H4 | 24px | 32px | 500 | 小标题 |
| H5 | 20px | 28px | 500 | 较小副标题 |
| H6 | 16px | 24px | 500 | 细节小标题 |

### 正文（Body Text）

| 类型 | 字体大小（px） | 行高（px） | 字重（Weight） | 用途描述 |
| --- | --- | --- | --- | --- |
| 正文大号 | 20px | 28px | 400 | 适用于重要段落或说明文字 |
| 正文常规 | 16px | 24px | 400 | 适用于普通段落、正文内容 |
| 正文小号 | 14px | 22px | 400 | 用于辅助信息、次要描述 |
| 小号辅助文字 | 12px | 18px | 400 | 用于注释、标签或说明 |

### 按钮（Buttons）

| 类型 | 字体大小（px） | 行高（px） | 字重（Weight） | 用途描述 |
| --- | --- | --- | --- | --- |
| 大按钮文字 | 18px | 24px | 500 | 主要操作按钮 |
| 常规按钮文字 | 16px | 22px | 500 | 一般操作按钮 |
| 小按钮文字 | 14px | 20px | 500 | 次要操作按钮 |

### **行高（Line Height）**

行高应确保文本在不同行级别中保持清晰的可读性和视觉上的呼吸感。推荐的行高比例是 **字体大小的 1.25 倍到 1.5 倍**，视具体设计需求而定。上表中的行高已根据此原则计算。

- **标题行高**：通常设置为字体大小的 **1.25 倍**，以确保标题有足够的间距但不至于过于松散。
- **正文行高**：建议设置为字体大小的 **1.5 倍**，以确保段落文本有良好的可读性。

### **字重（Font Weight）**

字重（字体粗细）在文字层级中起到区分作用。思源黑体提供了多个权重选项，常用的有：

- **Regular (400)**：常规字体，用于正文和长文段落。
- **Medium (500)**：略粗的字体，适合小标题、按钮文字、以及强调的正文。
- **Bold (600)**：用于大标题或需要特别强调的信息。
- **Extra Bold (700)**：主要用于页面主标题或视觉上非常突出的部分。

### 字重的具体使用建议：

| 字重 | 用途描述 |
| --- | --- |
| **400** | 正文、辅助性文字、一般说明 |
| **500** | 小标题、次要标题、按钮文字 |
| **600** | 重要副标题、强调内容 |
| **700** | 页面主标题、重要视觉元素 |

### **字间距（Letter Spacing）**

字间距是指字符之间的间距，通过合理设置字间距可以增强可读性或视觉美感。

- **标题字间距**：通常为 **0px**，但对于大号标题可以稍微调大（例如 **0.5px**），以增强视觉效果。
- **正文字间距**：保持在 **0px** 或 **0.25px**，确保内容紧凑但不失可读性。
- **小号文字或辅助文字**：可以适当增加字间距（例如 **0.25px**），避免字母过于紧密影响阅读。

### **颜色（Colors）**

文字颜色应根据背景和层次来选择，确保有足够的对比度和可读性。推荐遵循**WCAG 2.1** 标准，确保文字与背景有良好的对比度。

- **主文本颜色**：`#333333`（深灰色，适合正文内容）
- **次文本颜色**：`#666666`（浅灰色，用于次要或辅助信息）
- **提示文本颜色**：`#999999`（更浅的灰色，用于提示信息、占位符等）
- **反向文本颜色**：`#FFFFFF`（白色，适用于深色背景的文本）

## 色彩规范

这里主要叙述固定的几个颜色。

### 主色

- APP 的主色调是黄色 (`#F3C60E`)。
- 用途：主色用于应用的**品牌识别**，以及**主要交互组件**（如按钮、链接、图标等）的视觉设计。它是应用中最具辨识度的颜色，贯穿整个设计系统。
- **主要使用场景**：
    - **主要按钮**（Primary Button）
    - **选中状态**（Active State）
    - **进度条、加载动画**
    - **导航栏、页眉或页脚的背景色**
    - **品牌标识元素**

### **辅助色**

- **点睛色**
    - **颜色值**：`#FE7001`
    - **用途**：点睛色用于**强调和吸引用户注意力**的部分，如**次要按钮**、**通知**、**图标**或其他需要视觉重点的地方。点睛色可以与主色搭配使用，以增强视觉层次感。
    - **主要使用场景**：
        - **次要按钮**（Secondary Button）
        - **悬停状态**（Hover State）
        - **提示或警示**（如轻度的提醒、通知）
        - **图标点缀**

### 中性色

- 浅色背景
    - 颜色值：`#F5F5F5`
    - 用途：用于 APP 的画布背景。
- 组件浅色背景
    - 颜色值：`#FFFFFF`
    - 用途：用于组件的浅色背景。

## 圆角规范

- 多数圆角的曲率都使用 `16px` 。