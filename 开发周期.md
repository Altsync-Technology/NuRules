# 0. 获取一个任务

- 从 [任务](https://www.notion.so/11cd210df36f80b19753d89c731efd31?pvs=21) 中获取一个任务。
- 如果任务列表里有你负责的、且未完成的任务，你可以开始做这个任务。
- 如果任务列表没有你负责且未完成的任务，你可以领取一个任务——给一个任务的负责人添加你的名字。
- 如果你在添加负责人的时候，发现已有一个负责人，不要忘了去找这个负责人，讨论这个任务的信息。
- 你需要根据任务板的用法，来声明你正在做这个任务！

# 1. 从 `master` 分支创建一个新分支

将分支命名为 **分支类型/分支目标** 格式，例如：
`bugfix/page_not_found`

# 2. 编写代码

你可以通过几种方式在 Notion 中链接到其他页面：

- 输入 `/link`，按 `enter`，然后输入你想要的页面名称。
- 如果要内联创建链接，输入 `@`，然后跟上页面的标题，再按 `enter`。

# 3. 在 Github 上创建一个 Pull Request

在 PR 描述中包含 Notion 任务的链接。

# 4. 等待审核

- 审核通过后，在任务列表中把当前任务标记为 已完成。