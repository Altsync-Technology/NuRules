# 直接 commit 的情况
- 只有核心成员被允许直接 commit。
- 只有紧急修复的情况允许直接 commit。

普通情况下，请根据以下流程提交。

# 自审核
- 对于部分被许可的成员，允许在合理的情况下自审核自己的 Pull Request。

# 从 `master` 分支创建一个新分支

将分支命名为 **分支类型/分支目标** 格式，例如：
`bugfix/page_not_found`
```bash
git checkout master
git pull origin master
git checkout -b bugfix/page_not_found
```

# 在新分支编写和提交代码
* 开发过程中请保持分支单一主题，避免多功能混杂。
* 编码结束后，git add、git commit 并推送到远程：
```bash
git add .
git commit -m "修复404页面找不到的问题"
git push origin bugfix/page_not_found
```

# 在 Github 上创建一个 Pull Request
* 在 PR 描述中指向具体的任务板位置。
* 由核心成员进行代码审核。如果有改进建议或问题，会在 PR 下方评论。
* 根据反馈修改代码后，重新推送到同一分支，PR 会自动更新。
* 审核通过后，核心成员会合并 PR。
* 每个 PR 只关联一个主题/任务，避免“巨型合并”。
* PR 标题和描述务必规范、准确，便于团队协作和追踪。

# Pull Request内容格式规范

## app前端Pull Request内容格式说明
### PR模板
```bash
## TL;DR

## 关联 issue 或任务

## 原因

## 改动

## UI 修改截图
没有 UI 修改，故无截图。

## 发布通道
*不填写默认不自动打包发布，只填写一个*

## @mentions
*希望对PR进行审查的人员和团队*

## 额外信息
```
### “TL;DR” 格式要求

**根据Pull Request内容需求选择对应类型填写，选择一个填写。没有填写类型时，不会自动更新版本**

#### 更新版本号的类型：
- `bugfix`/`fix` - 修复bug (更新patch版本，如 0.0.0->0.0.1)
- `feature`/`feat` - 添加新功能 (更新minor版本，如 0.0.0->0.1.0)
- `perf` - 性能优化 (更新patch版本，如 0.0.0->0.0.1)
- `refactor` - 重构代码 (更新patch版本，如 0.0.0->0.0.1)
- `BREAKING CHANGE` - 破坏性更改 (更新major版本，如 0.0.0->1.0.0)
    - 删除或修改现有API
    - 更改数据库结构导致数据迁移
    - 移除某大型已有功能
    - 更改应用最低版本要求等

#### 不更新版本号的类型（维护性工作时选择）：
- `style:` - 代码格式调整，空格、分号等
- `docs:` - 文档更新和注释修改
- `test:` - 添加或修改测试用例
- `chore:` - 构建工具和依赖更新等杂项
- `ci:` - CI/CD 配置修改
- `build:` - 构建系统配置修改
- 或者不在TL;DR内填写类型前缀

#### TL;DR 填写示例：
```bash
# 单个改动
## TL;DR
feat: 添加用户登录功能

# 多个改动
## TL;DR
fix: 
- 修复用户登录功能
- 修复商品详情页显示问题
- 处理某某函数逻辑混乱问题

# 不更新版本的情况
## TL;DR
docs: 更新API文档

## TL;DR
更新代码注释和文档
```

### “发布通道” 填写要求
**只能填写一个发布通道，不填写时不进行自动打包发布**
- `alpha` - 内部测试通道
- `beta` - 灰度或其他对外测试通道
- `rc` - 最终测试通道
- `release` - 正式发布通道

注意：误填写多个时以第一个为准

#### 发布通道 填写示例：
```bash
# 内部测试
## 发布通道
alpha

# 正式版
## 发布通道
release

# 不发布时
## 发布通道
填写"不发布"或不填写内容
```

# 等待审核

- 审核通过后，在任务列表中把当前任务标记为 已完成。

# 分支用法
要添加新功能或者修复bug，现在要用分支。每一个分支维护一个主题，直到这个主题做完再合并

分支名称格式
[类型]/[具体需求]

```
例如
bugfix/fix_scroll_issue
feature/userpage
update/update to Java 21
```

功能合并后，删除本地和远程分支 (通常这时自动完成的)：
```bash
# 如果你的分支在本地
# 不要忘记推送标签和删除分支
git push --tags
git push origin --delete feature/historyfix
git branch -d feature/historyfix
```

参见 [Github 工作流](https://docs.github.com/zh/get-started/using-github/github-flow)

# 禁止直接 push
所有成员都禁止直接 push 或 commit 到主分支。
若发现违规操作，将视情况追责。
