# 直接 commit 的情况
- 只有核心成员被允许直接 commit。

对于未被允许直接 commit 的成员，请根据以下流程提交。

# 从 `master` 分支创建一个新分支

将分支命名为 **分支类型/分支目标** 格式，例如：
`bugfix/page_not_found`
```bash
git checkout master
git pull origin master
git checkout -b bugfix/page_not_found
```

# 在新分支编写和提交代码
* 开发过程中请保持分支单一主题，避免多功能混杂。
* 编码结束后，git add、git commit 并推送到远程：
```bash
git add .
git commit -m "修复404页面找不到的问题"
git push origin bugfix/page_not_found
```

# 在 Github 上创建一个 Pull Request
* 在 PR 描述中指向具体的任务板位置。
* 由核心成员进行代码审核。如果有改进建议或问题，会在 PR 下方评论。
* 根据反馈修改代码后，重新推送到同一分支，PR 会自动更新。
* 审核通过后，核心成员会合并 PR。
* 每个 PR 只关联一个主题/任务，避免“巨型合并”。
* PR 标题和描述务必规范、准确，便于团队协作和追踪。

# 等待审核

- 审核通过后，在任务列表中把当前任务标记为 已完成。

# 分支用法
要添加新功能或者修复bug，现在要用分支。每一个分支维护一个主题，直到这个主题做完再合并

分支名称格式
[类型]/[具体需求]

```
例如
bugfix/修复首页无法滚动的问题
feature/个人页面
update/更新到Java 21
```

功能合并后，请为分支打 archive 标签，并删除本地和远程分支：
```bash
# 如果你的分支在本地
git tag archive/功能实现/历史记录 功能实现/历史记录

# 如果分支在远程，那么指定的分支前面还需要加 remote/
git tag archive/功能实现/历史记录 remotes/origin/功能实现/历史记录

# 不要忘记推送标签和删除分支
git push --tags
git push origin --delete 功能实现/历史记录
git branch -d 功能实现/历史记录
```

参见 [Github 工作流](https://docs.github.com/zh/get-started/using-github/github-flow)

# 禁止直接 push
所有成员都禁止直接 push 或 commit 到主分支。
若发现违规操作，将视情况追责。