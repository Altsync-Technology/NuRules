# 后端结构

### 1. **后端四项**

### 1.1 **Common**

- **说明**：该模块存放项目中的模型类，通常是一些数据结构，不包含业务逻辑。
- **用途**：主要用于定义数据模型，以便在整个项目中进行数据传递。

### 1.2 **Service**

- **说明**：存放项目的主要业务逻辑类。这里包含了服务类（或工具类），这些类负责具体的业务实现。服务类的功能包括：
    - **Utils**：通用的工具类，提供一些常用的功能函数。
    - **Spring Boot 组件或服务**：注入到 Spring Boot 中的业务服务，处理业务操作、数据库交互以及调用其他服务的逻辑。

### 1.3 **Admin**

- **说明**：该模块包含与管理相关的控制器，通常用于管理端的接口和业务逻辑。管理员可以通过这些接口对系统进行管理操作，如用户管理、配置管理等。
- **用途**：为管理系统前端提供 API 接口。

### 1.4 **User**

- **说明**：该模块包含与用户端相关的控制器，通常用于处理用户的业务请求和逻辑，如用户登录、注册、个人信息管理等。
- **用途**：为用户系统前端提供 API 接口。

### 2. **依赖关系**

- **Service 依赖 Common**：业务逻辑类依赖数据模型类，即服务类在执行业务逻辑时，会依赖 Common 模块中的模型类来传递和处理数据。
- **Admin 和 User 依赖 Service 及 Common**：
    - 管理和用户控制器都依赖 Service 模块，调用业务逻辑服务来实现具体的业务操作。
    - 同时，它们还直接或间接依赖 Common 模块，用于数据模型的传递和操作。

---

# 前端结构

### 1. **前端目录结构**

### 1.1 **/lib**

- **说明**：该目录存放 Flutter 项目的主要 UI 代码，负责应用的页面展示和业务逻辑交互。Flutter 的所有开发工作基本都在该目录中进行。
- **子目录结构**：
    - **/common**：存放全局样式文件，包括颜色、字体、主题等。
        - **用途**：为整个应用提供一致性的视觉样式，确保不同页面和组件遵循统一的设计规范。
    - **/network**：存放网络请求的代码，特别是与 API 交互的函数。
        - **用途**：负责处理与后端的 HTTP 请求，封装 API 调用逻辑，确保前端与后端数据交互的正常进行。
    - **/pages**：存放应用的各个页面文件，每个页面代表应用中的一个视图或功能模块。
        - **用途**：每个页面对应应用中的一个具体功能或视图（如登录页面、用户页面等）。
    - **/service**：存放业务服务代码，通常处理较为复杂的业务逻辑或状态管理。
        - **用途**：封装前端的业务逻辑，管理应用状态，使页面能够更简单地与数据或其他模块交互。
    - **/utils**：存放通用的功能函数和类。
        - **用途**：封装并复用常用的工具方法，如格式化函数、日期处理函数、字符串处理等，避免代码重复。
    - **/widgets**：存放自定义的 Flutter 组件，通常是可以在多个页面中复用的 UI 组件。
        - **用途**：通过组件化开发，减少重复代码，并确保 UI 元素的一致性和可复用性。

### 1.2 **/ios**

- **说明**：存放 iOS 平台的相关代码和配置文件。这个目录主要包含 iOS 项目所需的资源和配置，允许 Flutter 应用在 iOS 设备上运行。
- **用途**：处理与 iOS 系统相关的配置和平台特性（如权限、推送通知等）。

### 1.3 **/android**

- **说明**：存放 Android 平台的相关代码和配置文件。这个目录包含 Android 项目所需的资源、Gradle 配置等，允许 Flutter 应用在 Android 设备上运行。
- **用途**：处理与 Android 系统相关的配置和平台特性（如权限、推送通知等）。

### 2. **前端依赖关系**

- **/network 依赖 /utils**：网络请求可能依赖一些通用的工具函数（如数据格式化、错误处理等）。
- **/pages 依赖 /widgets、/service 及 /network**：
    - 页面通过调用服务类（/service）来处理业务逻辑，并通过网络模块（/network）与后端进行通信。
    - 页面也会通过复用自定义组件（/widgets）来构建用户界面，确保一致的 UI 体验。
- **/widgets 依赖 /common**：自定义组件通常会依赖全局样式文件（/common），确保组件的样式与应用的整体设计保持一致。

---

## 依赖关系总览

- **后端依赖关系**：
    - **Service 依赖 Common**：服务类使用模型类来处理业务逻辑。
    - **Admin 和 User 依赖 Service 和 Common**：控制器依赖服务类来处理业务逻辑，并使用模型类传递数据。
- **前端依赖关系**：
    - **/pages 依赖 /widgets、/service 和 /network**：页面通过复用组件和调用服务类来构建视图，并通过网络模块与后端交互。
    - **/widgets 依赖 /common**：组件依赖全局样式文件来确保一致的视觉风格。
    - **/network 依赖 /utils**：网络请求代码依赖一些通用工具函数来处理数据。
- **前后端依赖关系**：
    - **前端 /network 模块依赖后端 Admin 和 User 的 API**：前端的网络模块通过 API 与后端的 Admin 和 User 控制器进行通信，获取或提交数据。

---

## 总结

- **后端结构**：后端由四个主要模块组成，分别负责数据模型（Common）、业务逻辑（Service）、管理端控制器（Admin）和用户端控制器（User）。后端的依赖关系体现为 Service 依赖 Common，而 Admin 和 User 控制器依赖 Service 和 Common。
- **前端结构**：前端由多个模块组成，主要集中在 `lib` 目录下，负责构建应用的 UI、处理网络请求和业务逻辑。通过模块化的设计，前端代码依赖关系清晰，页面和组件可以复用服务和工具函数，确保代码的简洁性和可维护性。
- **前后端关系**：前后端通过 API 进行交互，后端提供的 Admin 和 User 控制器为前端的网络模块（/network）提供数据支持，确保整个应用的功能正常运行。

---

通过这个项目结构说明，团队成员可以对前端和后端的组织方式、依赖关系有清晰的认识，便于开发和维护。